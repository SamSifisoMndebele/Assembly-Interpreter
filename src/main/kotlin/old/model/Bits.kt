package old.model

/**
 * Enum representing the number of bits for a data type.
 * This is used to specify the size or precision of numerical values.
 */
enum class Bits {
    /** Represents an 8-bit data type. */
    B8,

    /** Represents a 16-bit data type. */
    B16,

    /** Represents a 32-bit data type. */
    B32,

    /** Represents a neutral or unspecified bit size. */
//    NONE,
}